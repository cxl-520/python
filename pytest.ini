[pytest]
testpaths = testcase
norecursedirs = .* .venv build dist
# 其他原有配置（如testpaths、python_files等）保留，新增以下行
pythonpath = common/  # 将夹具所在目录加入Python搜索路径
# 用例识别规则（建议保留，确保pytest能找到用例）
python_files = test_*.py
python_functions = test_*
python_classes = Test*